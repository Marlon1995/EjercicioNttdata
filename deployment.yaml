apiVersion: v1
kind: Namespace
metadata:
  name: dev-devops-microservice

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-devops-microservice
  namespace: dev-devops-microservice
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dev-devops-microservice
  template:
    metadata:
      labels:
        app: dev-devops-microservice
    spec:
      containers:
      - name: dev-devops-microservice
        image: kfcregistry.azurecr.io/devops-microservice:dev
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "512Mi"
            cpu: "1"
          requests:
            memory: "256Mi"
            cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: dev-devops-microservice
  namespace: dev-devops-microservice
spec:
  selector:
    app: dev-devops-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
